# Secrets. `.env.example` files are templates and belong in the repo.
.env
*.env.*
!*.env.example

# Local scratch
test.ts

# Dependencies and build output
node_modules/
dist/

# Design handoff bundle, if it is ever copied in
permis-munca-login-design/
